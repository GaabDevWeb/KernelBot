# Copiar para .env.staging.local (não commitar secrets reais de produção).
# Valores alinhados com docker-compose.staging.yml (dev only).

OPENROUTER_API_KEY=staging-dummy-not-used-for-e2e

DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=kernelbot_staging
DB_USER=kb_staging
DB_PASSWORD=kb_staging_pw

ACL_GLOBAL_CONTEXT=geral
ACL_CATALOG_ENABLED=false
