name: KernelBot Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}

permissions:
  contents: write

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          committer_token: ${{ secrets.METRICS_TOKEN }}
          user: GaabDevWeb
          repo: KernelBot
          template: repository
          base: header, metadata
          config_timezone: America/Sao_Paulo

          # Cartão de apresentação do repositório
          plugin_introduction: yes
          plugin_introduction_repository: yes

          # Distribuição de linguagens
          plugin_languages: yes
          plugin_languages_details: percentage
          plugin_languages_indepth: yes
          plugin_languages_analysis_timeout: 15

          # Linhas de código
          plugin_lines: yes

          # Crescimento de estrelas (gráfico visual)
          plugin_stargazers: yes
          plugin_stargazers_charts_type: graph

          # Contribuidores com avatares
          plugin_contributors: yes
          plugin_contributors_contributions: yes

          # Acompanhamento de issues e PRs
          plugin_followup: yes
          plugin_followup_sections: repositories

          # OUTPUT
          filename: kernel-status.svg
          output_action: commit
          committer_branch: main
