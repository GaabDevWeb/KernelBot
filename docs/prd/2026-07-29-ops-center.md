# PRD — Central de Operações do Kernel (P0)

| Campo | Valor |
|-------|-------|
| Data | 2026-07-29 |
| Status | in progress (P0) |
| Branch | `feature/kernel-ops-center` |
| Stack UI | FastAPI + Jinja + HTMX leve (sem React) |

## P0 (esta entrega)

Dashboard executivo, Operações (Traces existentes, Logs, Sistema, Métricas), shell de navegação completa com placeholders P1–P4.

## Critério P0

Operar diagnóstico básico só pelo painel: dashboard + traces + sistema + métricas + logs derivados de traces/erros.
